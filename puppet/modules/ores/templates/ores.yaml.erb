SERVER_NAME: <%= @vhost_name %>:<%= @port %>

ores:
  scoring_system: local_celery

scoring_systems:
  defaults:
    scoring_contexts:
      - wiki

scoring_contexts:
  wiki:
    extractor: testwiki_offline
    scorer_models:
      damaging: testwiki_revid
    precache:
      damaging:
        "on": ['edit']

