# vim: filetype=apache sts=4 sw=4 autoindent
#
# Apache site configuration for <%= @site %>
# This file is managed by Puppet.
#
<VirtualHost <%= Array(@listen).join(' ') %>>
  Include site.d/<%= @site %>
</VirtualHost>
