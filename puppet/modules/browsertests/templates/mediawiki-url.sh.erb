#!/bin/sh
# Set 'MEDIAWIKI_URL' environment variable, used by the Selenium browser
# tests to determine which MediaWiki instance to run against.
#
# This file is managed by Puppet.
#
export MEDIAWIKI_URL="<%= @mediawiki_url %>"
