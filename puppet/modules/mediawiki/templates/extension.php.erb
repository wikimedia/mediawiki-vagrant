if ( class_exists( 'ExtensionRegistry' ) && file_exists( "$IP/extensions/<%= @ext_name %>/extension.json" ) ) {
	wfLoadExtension( '<%= @ext_name %>' );
} else {
	include_once "$IP/extensions/<%= @ext_name %>/<%= @ext_entrypoint %>";
}

