The '''lockdown''' role makes a wiki into a fishbowl (anons can read by not edit) and also disables account creation via the wiki itself. Accounts can still be created using maintenance scripts such as <code>createAndPromote.php</code>.
