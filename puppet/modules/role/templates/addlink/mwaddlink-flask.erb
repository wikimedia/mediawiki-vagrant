#!/bin/bash
# THIS FILE IS MANAGED BY PUPPET
DB_BACKEND=mysql \
    DB_DATABASE=<%= @db_name %> \
    DB_USER=<%= @db_user %> \
    DB_PASSWORD=<%= @db_pass %> \
    MEDIAWIKI_API_URL=${MEDIAWIKI_API_URL:-<%= @server_url %>/w/rest.php} \
    MEDIAWIKI_API_BASE_URL=${MEDIAWIKI_API_BASE_URL:-<%= @server_url %>/w/} \
    FLASK_APP=<%= @service_dir %>/app \
    FLASK_DEBUG=1 \
    <%= @venv_dir %>/bin/flask "$@"
