[Unit]
Description="cgrulesengd"

[Service]
User=root
Group=root
ExecStartPre=/usr/sbin/cgconfigparser -l /etc/cgconfig.conf
ExecStart=/usr/sbin/cgrulesengd --nodaemon
Restart=on-failure

[Install]
WantedBy=multi-user.target
