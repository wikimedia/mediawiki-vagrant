output {
<%- if @require_tag %>if "<%= @require_tag %>" in [tags] {<% end -%>
    elasticsearch {
        hosts              => ["<%= @host %>:<%= @port %>"]
        index              => "<%= @index %>"
    }
<% if @require_tag %>}<% end %>
}
