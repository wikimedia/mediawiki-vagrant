#!/bin/bash

<%= scope['::crm::drush::wrapper'] %> \
    site-install standard \
    --db-url='<%= @db_url %>' \
    --site-name='<%= scope['::crm::site_name'] %>' \
    --account-name=admin --account-pass='vagrant' \
    install_configure_form.update_status_module='array(FALSE,FALSE)'
