---
:backends:
  - yaml
:yaml:
  :datadir: /vagrant/puppet/hieradata
:hierarchy:
  - local
  - vagrant-managed
  - "environment/%{::environment}"
  - "provider/%{::provider_name}"
  - common
:logger: console
